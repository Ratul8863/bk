'use client';

import { SettingsEditorPage } from '@/components/admin/SettingsEditorPage';

export default function Page() {
  return (
    <SettingsEditorPage
      title="Organisation profile"
      description="Name, mission, vision, and other organisation wording shown on the public site."
    />
  );
}
